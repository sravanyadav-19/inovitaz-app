/**
 * Legal document "last updated" dates.
 *
 * These are intentionally hard-coded. Do NOT use `new Date()` here — that
 * would silently reset the displayed date to "today" for every visitor,
 * instead of reflecting when the policy was actually revised.
 *
 * To update a policy: edit the document text, then bump the date below.
 */
export const LEGAL_LAST_UPDATED = "13 August 2026";
